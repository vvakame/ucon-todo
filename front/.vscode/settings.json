// Place your settings in this file to overwrite default and user settings.
{
    "files.exclude": {
        "**/.git": true,
        "**/.DS_Store": true,
        "src/**/*.js": { "when": "$(basename).ts" },
        "src/**/*.js.map": true
    }
}